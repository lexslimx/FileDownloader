namespace FileDownloader.Core.Utils
{
    public enum FileStatus
    {
        Ready = 1,
        Accepted = 2,
        Rejected = 3
    }

    public enum FileType
    {
        Image = 1,
        Text = 2,
        Html = 3,
        Other = 4
    }
}